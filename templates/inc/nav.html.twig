
<nav class="navbar navbar-expand-lg bg-body-tertiary">
    <div class="container-fluid d-flex flex-row justify-space-between">
        <div class="p-2">
            <a href="{{ path('main_accueil') }}">
                <img class="navbar-brand" src="{{ asset('images/logo.webp')}}" alt="logo_sortir" width="200 px">
            </a>
        </div>
        <div class="p-2">
            {% if is_granted('ROLE_PARTICIPANT') %}
                <div class="collapse navbar-collapse me-auto mb-2 mb-lg-0" id="navbarNav">
                    <ul class="navbar-nav">
                        {% if is_granted('ROLE_ADMIN') %}
                            <li class="nav-item">
                                <a class="nav-link" href="#">Villes</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="#">Campus</a>
                            </li>
                        {% endif %}
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('main_accueil') }}"><strong>Accueil</strong></a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('profil_modifier') }}"><strong>Mon profil</strong></a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_logout') }}"><strong>Se déconnecter</strong></a>
                        </li>
                    </ul>
                </div>
            {% endif %}
        </div>
    </div>
</nav>